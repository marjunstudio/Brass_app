<% @musics.each do |music| %>
  <li class="list-group-item cursor-pointer hover:bg-gray-200 border-b-2 border-gray-200 p-2" role="option" data-autocomplete-value="<%= music.id %>" data-autocomplete-label="<%= music.title %>">
    <%= music.title %>
  </li>
<% end %>
